Given an array A of length N. Also given are integers B and C.

Return 1 if there exists a subarray with length B having sum C and 0 otherwise

Code:
class Solution:
    # @param A : list of integers
    # @param B : integer
    # @param C : integer
    # @return an integer
    
    def solve(self,A, B, C):
        #A = array given of length N
        #B = window length
        #C = check the sum of subarray
        sub_sum = 0
        for i in range(B):
            sub_sum+=A[i]
            if sub_sum==C:
                return 1

        i = 1
        j = B
        while j<len(A):
            sub_sum = sub_sum + A[j] - A[i-1]
            if sub_sum ==C:
                return 1
            i+=1
            j+=1
        
        return 0

Given a string A, you are asked to reverse the string and return the reversed string.

Code:
class Solution:
    # @param A : string
    # @return a strings
    def solve(self, A):
        B = list(A)
        N = len(B)
        i = 0
        j = N-1

        while i<j:
            B[i],B[j] = B[j],B[i]
            i+=1
            j-=1
        return ''.join(B)
        
